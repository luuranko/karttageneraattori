package karttageneraattori.Logic;
/**
 * TileType is an enum class defining types for tiles in the map.
 * Empty types are Tiles that have not yet been processed.
 * <p>
 */
public enum Type {
    EMPTY,
    SEA,
    LAND,
    LAKE;
}
